/**
 * DevLoop — shared constants
 */

/** Custom entry type used to persist devloop state across sessions */
export const ENTRY_TYPE = "devloop-state";

/** Widget ID for the phase progress bar shown above the editor */
export const WIDGET_ID = "devloop-progress";
